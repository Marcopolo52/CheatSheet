# **Exposed**

```kotlin
//Configure connection to use later
val postgreSQL = Database.connect(
    url = null,
    driver = null,
    user = null,
    password = null,
    databaseConfig = DatabaseConfig{ null; }
)
```


